

export interface MarketInfoModel {
    name: string,
    bonus: string,
    cropId: number,
    cropIdIncrement: number,
    cost: number,
    costExponent: number,
    cropReq: number,
    maxLvl: number,
    bonusPerLvl: number
}
